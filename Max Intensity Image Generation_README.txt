*** Automation of maximum intensity image generation

This macro takes a microscope file as an input, and generates a directory with the same name as the output. It then converts the file to Tiff, generates a maximum intensity projection of all fluorescent channels, and saves them as separate files. 
